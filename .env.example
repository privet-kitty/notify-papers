# Local development environment variables
# Copy this to .env and modify as needed

# Required: Email configuration
EMAIL_RECIPIENT=test@example.com

# Required: S3 bucket name (created automatically by S3Mock)
S3_PAPERS_BUCKET=report-papers-local

# Required: Research topics (comma-separated)
RESEARCH_TOPICS=algorithmic game theory,discrete convex analysis

# Required: ArXiv categories (comma-separated)
ARXIV_CATEGORIES=cs.AI,cs.LG,cs.CL

# AWS Configuration
AWS_BEDROCK_REGION=us-east-1

# Translation (set to "en" to disable translation)
TRANSLATE_TARGET_LANGUAGE=en

# Local development endpoints (for Docker Compose services)
S3_ENDPOINT=http://localhost:9090
SES_ENDPOINT=http://localhost:8005

# Optional: Translate endpoint (not used when TRANSLATE_TARGET_LANGUAGE=en)
# TRANSLATE_ENDPOINT=

# Optional: Override default LLM model
# LLM_MODEL=anthropic.claude-3-haiku-20240307-v1:0

# Optional: Override search parameters
# MAX_RESULTS_PER_TOPIC=20
# DAYS_BACK=3
# RELEVANCE_THRESHOLD=0.7
# MAX_PAPERS_PER_EMAIL=10